[project]
name = "lyrpay-backend"
version = "0.1.0"
description = "Backend API for Lyrpay Web3 trading platform"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.111.0",
  "uvicorn[standard]>=0.23.0",
  "httpx>=0.26.0",
  "python-dotenv>=1.0.0",
  "pydantic>=2.8.0",
  "mangum>=0.18.0"
]

[tool.poetry]
name = "lyrpay-backend"
version = "0.1.0"
